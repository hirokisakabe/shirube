---
"shirube": major
---

Replace separate goals and reviews with weekly cycles that store goal and review content together per ISO week.

Existing `goals` and `reviews` tables are dropped by the migration instead of being migrated into the new schema.
