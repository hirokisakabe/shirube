{
  "name": "@uchi/web",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc && node -e \"const fs=require('fs'); fs.cpSync('public', 'dist', {recursive: true})\"",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^4.1.8"
  }
}
