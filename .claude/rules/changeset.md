# Changeset ルール

ユーザーに影響する変更を実装した後、PR 作成前に必ず `pnpm changeset` を実行すること。

## バージョンの使い分け

- **patch**: バグ修正・内部的な小改善（後方互換あり）
- **minor**: 後方互換のある新機能追加
- **major**: 破壊的変更（後方非互換）

## changeset が不要なケース

以下のみの変更は changeset を追加しなくてよい：

- リファクタリング（ユーザー向け動作の変化なし）
- テストの追加・修正
- ドキュメントのみの変更
